{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{'Création de la note (/5):'|trans}} </h1>
    
    </div>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    

    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default">{{'Enregistrer'|trans}}</button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}